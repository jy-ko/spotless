<h1>Editing Task</h1>

<%= simple_form_for [ @task, @room ] do |f| %>
  <%= f.input :content %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>

<%= link_to 'Show', @room %> |
<%= link_to 'Back', room_path(@room) %>